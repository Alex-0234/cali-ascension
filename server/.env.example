PORT=5000
NODE_ENV=development

MONGO_URI=
SALT_ROUNDS=10
JWT_SECRET=

# Public demo account. It resets instead of being deleted, so visitors can't
# remove it. Leave unset to default to "test"; set to something unused to
# disable the protection entirely.
DEMO_USERNAME=test

LOCALHOST_URL=http://localhost:5173
VERCEL_URL=
VERCEL_BRANCH_URL=
